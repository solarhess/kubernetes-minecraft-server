apiVersion: v1
kind: Service
metadata:
  name: minecraft-server
  namespace: minecraft
  labels:
    app: minecraft-server
spec:
  type: LoadBalancer
  ports:
    - protocol: TCP
      port: 25565
      targetPort: {{ .Values.minecraft.port }}
  selector:
    app: minecraft-server
