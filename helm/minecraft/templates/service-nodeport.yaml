apiVersion: v1
kind: Service
metadata:
  name: minecraft-server
  namespace: minecraft
  labels:
    app: minecraft-server
spec:
  type: NodePort
  ports:
    - port: 30565
      nodePort: {{ .Values.minecraft.port }}
  selector:
    app: minecraft-server
